import PreEventRegForm from '@/components/PreEventRegForm'

const PreEventReg = () => {
  return (
    <div className='w-full min-h-screen bg-[#430B04] flex items-center justify-center'>
      <PreEventRegForm/>
    </div>
  )
}

export default PreEventReg
